# ===== 开发与测试依赖（不进入生产镜像）=====
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0  # 用于 TestClient

# 代码质量
ruff>=0.5.0
mypy>=1.10.0

# AI 评测（仅 CI 评测环境）
deepeval>=1.0.0
