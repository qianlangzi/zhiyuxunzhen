# ===== FastAPI 核心 =====
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
python-multipart==0.0.9

# ===== 大模型（OpenAI 兼容协议，可对接星火/通义/DeepSeek/本地 vLLM）=====
openai==1.35.0

# ===== 向量库 =====
pymilvus==2.4.3
# 固定 marshmallow<4：environs(pymilvus 依赖) 需要 ma.__version_info__，marshmallow 4.x 已移除该属性，
# 会导致 import pymilvus 崩溃，进而使 Milvus 连接降级到内存索引。
marshmallow==3.23.2

# ===== PDF 解析 =====
pypdf==4.3.1
# PyMuPDF：清洗打标管道使用（字号识别 + 文本提取 + 图片提取，抗乱码）
PyMuPDF>=1.24.0

# ===== 混合检索（BM25 稀疏检索 + jieba 中文分词）=====
jieba>=0.42.1
rank_bm25>=0.2.2

# ===== HTTP 客户端 =====
httpx==0.27.0

# ===== 重试 =====
tenacity==8.2.3

# ===== Mobile JWT / 标准 SSE =====
PyJWT==2.10.1
sse-starlette==2.1.3

# ===== Redis (异步任务队列/幂等/checkpoint) =====
redis>=5.0.0

# ===== LangGraph (SP Agent 状态编排) =====
langgraph>=0.2.0
