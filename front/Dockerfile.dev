# 开发环境前端:直接跑 Vite dev server,支持 HMR 热更新
# 生产环境请用 Dockerfile (多阶段构建 → Nginx)
FROM node:20-alpine
WORKDIR /app

RUN npm config set registry https://registry.npmmirror.com

COPY package*.json ./
RUN npm ci || npm install

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
