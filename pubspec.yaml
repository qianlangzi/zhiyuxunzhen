name: zhiyuxunzhen
description: "智愈寻真 — 内科教研协同智能体平台"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # State management
  flutter_riverpod: ^2.6.1
  # Routing
  go_router: ^14.8.1
  # Network
  dio: ^5.7.0
  # Charts
  fl_chart: ^0.70.2
  # Local storage
  shared_preferences: ^2.3.4
  # Google Fonts
  google_fonts: ^6.2.1
  # Safe storage for JWT
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
